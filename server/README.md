# Notes

- Requires `darktable` to be installed and available on the host system — this handles raw image formats such as `.NEF`
  - Note that `darktable` will return the RAW image data, not the processed `.jpeg` preview image
